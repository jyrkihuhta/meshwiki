# Development dependencies for the factory orchestrator.
#
# Mirrors the `dev` extras in orchestrator/pyproject.toml so a plain
# `pip install -r requirements-dev.txt` works in CI containers and grinder
# sandboxes without requiring the package to be installed editable first.
#
# Keep this file in sync with [project.optional-dependencies].dev in
# orchestrator/pyproject.toml. cryptography is included here too: some
# test fixtures (notably ones that mirror the molly-armory contract test
# layout) import it at conftest collection time, and the orchestrator's
# own pytest collection can hit `ModuleNotFoundError: No module named
# 'cryptography'` on a fresh sandbox without it.
-r pyproject.toml
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
black>=24.0
isort>=5.13
ruff>=0.4
respx>=0.21
cryptography>=42.0
